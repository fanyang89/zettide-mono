# References

This directory contains optional upstream source checkouts used for protocol
comparison and interoperability testing. References are not part of the default
bootstrap, build, test, or update tasks.

- `EasyTier/` is pinned directly from the official upstream repository. Run
  `mise run reference:easytier` to initialize it with pushes disabled.
