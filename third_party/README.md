# Managed Third-Party Dependencies

This directory contains external source dependencies that the workspace builds
or links. They are pinned and updated deliberately but are not Zettide-owned
components.

- `spdk/` is the writable Zettide fork used by the Linux storage data plane.
