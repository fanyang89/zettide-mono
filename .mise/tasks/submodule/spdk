#!/bin/sh
#MISE description="Initialize the SPDK Git submodule"
#MISE hide=true
set -eu

root=$(CDPATH= cd -- "$(dirname -- "$0")/../../.." && pwd)
git -C "$root" submodule update --init --recursive -- third_party/spdk
