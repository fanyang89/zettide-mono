#!/bin/sh
#MISE description="Initialize the read-only EasyTier reference checkout"
set -eu

root=$(CDPATH= cd -- "$(dirname -- "$0")/../../.." && pwd)
git -C "$root" submodule update --init -- references/EasyTier
git -C "$root/references/EasyTier" remote set-url --push origin DISABLED
