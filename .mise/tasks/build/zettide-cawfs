#!/bin/sh
#MISE description="Build zettide-cawfs in ReleaseFast mode"
set -eu

root=$(CDPATH= cd -- "$(dirname -- "$0")/../../.." && pwd)
cd "$root/zettide-cawfs"
exec zig build -Doptimize=ReleaseFast
