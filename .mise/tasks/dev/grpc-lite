#!/bin/sh
#MISE description="Watch grpc-lite with incremental compilation"
set -eu

root=$(CDPATH= cd -- "$(dirname -- "$0")/../../.." && pwd)
cd "$root/grpc-lite"
exec zig build -fincremental --watch
