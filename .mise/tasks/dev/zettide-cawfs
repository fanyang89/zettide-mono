#!/bin/sh
#MISE description="Watch zettide-cawfs with incremental compilation"
set -eu

root=$(CDPATH= cd -- "$(dirname -- "$0")/../../.." && pwd)
cd "$root/zettide-cawfs"
exec zig build -fincremental --watch
