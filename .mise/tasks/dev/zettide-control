#!/bin/sh
#MISE description="Watch zettide-control with incremental compilation"
set -eu

root=$(CDPATH= cd -- "$(dirname -- "$0")/../../.." && pwd)
cd "$root/zettide-control"
exec zig build -fincremental --watch
